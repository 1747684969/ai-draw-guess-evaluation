{error && (
          <div className={styles.error}>
            ❌ {error}
          </div>
        )}

        {guess && (
          <div className={styles.result}>
            <h2 className={styles.resultTitle}>AI的猜测：</h2>
      
